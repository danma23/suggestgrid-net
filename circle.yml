machine:
  pre:
    - sudo apt-get update
    - sudo apt-get install mono-complete
test:
  override:
    - echo "test"
deployment:
  master:
    branch: master
    owner: suggestgrid
    commands:
      - ./.deploy
